# plan_0005 - 통합 검증 러너 및 회귀 테스트 정렬

## 상태
- 완료 (2026-02-07)

## 목표
- 정렬 작업 이후 실제 턴 루프가 안정 동작하는지 자동 검증 체계를 고정한다.

## 작업 단위
1. 테스트 경로 정리
- 대상: `tester/src/tester/client.py`, `tester/src/tester/runner.py`
- 조치:
  - 실제 API 경로/응답 형식 변화 반영
  - 실패 지점별 진단 로그 강화

2. 계약 기반 회귀 시나리오 추가
- Scenario inject 계약 검증
- GM turn/npc-turn 계약 검증
- state commit 결과 검증(commit_id, turn progression)

3. 기준선 확정
- 10턴 smoke / 20턴 endurance를 분리
- 실패 시 재현 커맨드 + 로그 파일 규칙 고정

## 완료 기준 (DoD)
- 10턴 기본 성공 + 20턴 장기 실행에서 치명 에러(500/commit 실패/스키마 mismatch) 0건.

## 실행 결과 (2026-02-07)
- 회귀 테스트 자동화 추가:
  - `tests/test_plan_0005_runner_regression.py`
  - 검증 항목:
    - 10턴 smoke 루프 성공
    - 20턴 endurance 루프 성공
    - 실패 재현 시 completed_turns/에러 리포트 검증
- 실행:
  - `cd services/gm && PYTHONPATH=../../tester/src uv run --group dev python -m pytest ../../tests/test_plan_0005_runner_regression.py -q`
  - 결과: `3 passed`

### GM 정합성 보강 (실환경 장애 대응)
- 문제 1: `check_scenario` 502 (`scenario_id=unknown`)
  - 원인: 실행 컨테이너 코드/최신 워크트리 불일치 + state fetch 경계 취약
  - 조치: GM 재빌드/재기동 및 외부 client 경계 정리
- 문제 2: `next_seq_id`가 `창고_탐색` 같은 비계약 값으로 상태 오염
  - 조치: GM에서 `act-*`/`seq-*` ID 정규화 및 invalid transition 무시
- 문제 3: Rule Engine 일시 500으로 턴 루프 중단
  - 조치: GM `check_rule`에 fallback 기본 판정 적용(룰엔진 코드 무변경)

### 추가 검증
- 단위 테스트:
  - `cd services/gm && uv run --group dev python -m pytest tests/test_http_clients_extended.py tests/test_pipeline_precision.py -q`
  - 결과: `11 passed`
- 실환경 스모크:
  - `PYTHONPATH=tester/src uv run python -m tester.runner smoke10 10`
  - 결과: 10턴 완료 (오류 0)
- 실환경 장기:
  - `PYTHONPATH=tester/src uv run python -m tester.runner endurance20 20`
  - 결과: 20턴 완료 (오류 0)
