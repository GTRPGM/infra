# plan_0027 - require_session_end 강제 장기 회귀

## 상태
- 완료 (2026-02-09)

## 목표
- `six_sequence_three_act_eval` 프로필에 대해 `require_session_end=True` 강제 모드로 다회 실행하여
  종료 안정성을 검증한다.
- 각 실행에서 시퀀스 전이/적 HP 감소/종료 상태(`ended`) 도달 여부를 기록한다.

## 작업 단위
1. 동일 시나리오를 재주입/고정 후 회귀 조건을 통일한다.
2. 다회 실행(세션별)로 `require_session_end=True` 검증을 수행한다.
3. 실패 시점의 로그를 수집해 종료 게이트 잔존 리스크를 분리한다.
4. 결과를 계획 문서와 평가 보고서에 반영한다.

## 수행 결과
- 실행 조건
  - 프로필: `six_sequence_three_act_eval`
  - 모드: `require_session_end=True`, `max_turns=20`
  - 반복 횟수: 3회 (세션별 독립 실행)

- 실행 결과 (요약)
  1. run1
     - `session_id`: `609e0254-4775-44a6-9003-f60c296fc86d`
     - `status=success`, `session_ended=true`, `total_turns=13`
     - `alive_enemies_in_current_sequence=0`
     - 로그: `log/test_output_endgate_reg_1_1770571910_20260209_023151.log`
  2. run2
     - `session_id`: `a34a3498-bb9a-425c-9568-d064fdb3e919`
     - `status=success`, `session_ended=true`, `total_turns=11`
     - `alive_enemies_in_current_sequence=0`
     - 로그: `log/test_output_endgate_reg_2_1770572085_20260209_023445.log`
  3. run3
     - `session_id`: `93151929-1e83-40ca-bb49-42c5972da9dc`
     - `status=success`, `session_ended=true`, `total_turns=11`
     - `alive_enemies_in_current_sequence=0`
     - 로그: `log/test_output_endgate_reg_3_1770572247_20260209_023727.log`

- 결론
  - 종료 게이트 보강 이후, 장기 회귀(3/3)에서 모두 `ended` 도달.
  - 종료 시점 현재 시퀀스 생존 적 수는 전부 `0`으로 일관.
