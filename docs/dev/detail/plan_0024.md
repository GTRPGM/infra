# plan_0024 - 잔존 운영 이슈 정리 (manifest/표시정합/session-add)

## 상태
- 완료 (2026-02-09)

## 목표
- 테스터 로드 시 시나리오 manifest(acts/sequences)가 비어 보이는 문제를 해소한다.
- 상태 스냅샷에서 세션 전체 엔티티 중복 표기를 줄여 현재 시퀀스 기준 가시성을 높인다.
- rule-engine `/session/add`의 `user_sessions` 테이블 미존재 500을 완화한다.

## 작업 단위
1. `tester`가 시나리오 목록 선택 후 상세(`/state/scenario/{id}`)를 재조회하도록 보강.
2. `runner` 상태 출력에서 NPC/적 목록은 `sequence` 스코프 우선 표시로 정렬.
3. `rule-engine` session service에서 `user_sessions` 미존재 시 graceful fallback.
4. 관련 테스트 실행 및 결과 문서 반영.

## 수행 결과
1. `tester` 시나리오 로드 정합 보강
   - `GMClient.get_scenario()` 추가.
   - `agent.setup_session()`에서 state 시나리오 상세를 재조회해 manifest(acts/sequences/npcs/enemies) 반영.
2. BE-router/state-manager 시나리오 상세 경로 복구
   - `GET /state/scenario/{scenario_id}` 프록시/DTO 확장.
   - `item.assigned_sequence_id` 컬럼 의존 쿼리 제거(`item.meta` 기반).
3. 인벤토리/아이템 매핑 루트코즈 수정
   - 시나리오 주입 시 `item.meta`에 `assigned_sequence_id(s)` 기록.
   - `item.meta` JSONB 이중 인코딩 제거(`json.dumps` 제거).
   - 시나리오 상세에서 시퀀스 아이템 매핑 정상화.
4. 상태 스냅샷 가시성 개선
   - `runner` HP 표기 로직 보강(`state.numeric.HP` 인식).
   - `state-manager` 현재 시퀀스 상세에 `items` 추가 + BE-router DTO 전달 경로 반영.
5. 검증
   - `services/state-manager` 전체 테스트: `143 passed`, `84%`.
   - 서비스 커버리지(BE-router/rule-engine 제외): `gm 87%`, `state-manager 84%`, `scenario-service 81%`, `llm-gateway 82%`.
   - 테스터(`six_sequence_three_act_eval`)에서 `seq-1 -> ... -> seq-6` 전이 및 등장/퇴장 검증 로그 확보.
   - 시작 인벤토리(포션/철검) 지급 및 시나리오 manifest의 시퀀스 아이템 표기 복구 확인.
6. 산출물
   - 평가/리스크 정리 문서: `docs/dev/detail/test_assessment_20260209_post_fix.md`
