# plan_0014 - 커버리지 미달 보강 및 실패 테스트 수정

## 상태
- 진행 중 (2026-02-08)

## 목표
- `gm` 유닛 테스트 실패 1건을 수정해 전체 테스트 통과 상태로 만든다.
- `state-manager`, `scenario-service` 유닛 커버리지를 각각 80% 이상으로 끌어올린다.
- 보강 범위는 프로덕션 코드 변경 최소화 원칙으로, 테스트 보강 중심으로 진행한다.

## 작업 단위
1. `gm` 테스트 실패 수정
- `tests/test_interfaces.py`의 `MockStateManager`에 누락된 추상 메서드 구현 추가

2. `state-manager` 커버리지 보강
- 저커버리지 모듈 중심 단위 테스트 추가:
  - `repositories/progress.py`
  - `repositories/trace.py`
  - `services/state_service.py`

3. `scenario-service` 커버리지 보강
- 저커버리지 모듈 중심 단위 테스트 추가:
  - `plugins/rule_engine/adapter.py`
  - `plugins/llm/adapter.py`의 미커버 분기(retry/structured-output/error-path)

4. 검증
- 각 서비스에서 `pytest --cov --cov-fail-under=80` 재실행
- 통과/실패 및 커버리지 수치 기록

## 완료 기준 (DoD)
- `gm` 테스트 실패 0건
- `state-manager` 커버리지 80% 이상
- `scenario-service` 커버리지 80% 이상
- 실행 결과가 본 문서에 기록됨

## 실행 결과 (2026-02-08)
### 1) `gm` 실패 테스트 수정
- 변경: `services/gm/tests/test_interfaces.py`
  - `MockStateManager.end_session()` 구현 추가
- 검증:
  - `cd services/gm && uv run --group dev python -m pytest tests/test_interfaces.py -q`
  - 결과: `2 passed`
  - 전체 재검증:
    - `cd services/gm && uv run --group dev python -m pytest tests --cov=gm --cov-report=term --cov-fail-under=80`
    - 결과: `61 passed`, 커버리지 `86.73%`

### 2) `state-manager` 커버리지 보강
- 추가 테스트:
  - `services/state-manager/tests/test_progress_repository_unit.py`
  - `services/state-manager/tests/test_trace_repository_unit.py`
  - `services/state-manager/tests/test_state_service_unit.py`
- 검증:
  - `cd services/state-manager && uv run --group dev python -m pytest tests --cov=state_db --cov-report=term --cov-fail-under=80`
  - 결과: `140 passed`, 커버리지 `83.36%`

### 3) `scenario-service` 커버리지 보강
- 추가 테스트:
  - `services/scenario-service/tests/test_rule_engine_adapter.py`
  - `services/scenario-service/tests/test_llm_adapter_extended.py`
  - `services/scenario-service/tests/test_interfaces_coverage.py`
- 검증:
  - `cd services/scenario-service && uv run --group dev python -m pytest tests --cov=scenario --cov-report=term --cov-fail-under=80`
  - 결과: `62 passed`, 커버리지 `80.35%`

### 4) 기준 재확인 (`llm-gateway`)
- 검증:
  - `cd services/llm-gateway && uv run --group dev python -m pytest tests --cov=llm_gateway --cov-report=term --cov-fail-under=80`
  - 결과: `27 passed`, 커버리지 `81.78%`

## 결론
- 제외 대상(`rule-engine`, `BE-router`)을 제외한 서비스의 유닛 커버리지가 모두 80% 이상으로 충족됨.
- `gm` 유닛 테스트 실패(인터페이스 목 누락)도 해소됨.

## 상태
- 완료 (2026-02-08)
