# plan_0006 - 단계적 실행 순서 및 롤아웃 정책

## 목표
- 정렬 작업을 안전하게 순차 적용하고, 영향 범위를 통제한다.

## 실행 순서
1. `plan_0007` 서비스 폴더(`services/`) 재배치 및 테스터 경로(`tester/src`) 최신화
2. `plan_0001` 계약 고정/감사
3. `plan_0002` Scenario -> State 정렬
4. `plan_0003` GM 중계 계약 정렬
5. `plan_0004` BE-router 경계 정렬
6. `plan_0005` 통합 검증/회귀 고정

## 변경 통제 규칙
- Rule Engine: 코드 변경 금지 유지
- State Manager: 오류 재현 증거 없으면 변경 금지
- 모든 단계는 서비스별 PR 단위로 분리 가능하도록 작업 묶음 유지

## 게이트 (각 단계 공통)
- 계약 테스트 통과
- 핵심 헬스체크 통과
- 통합 러너 스모크 성공

## 완료 기준 (DoD)
- Scenario Service / GM / BE-router / tester가 State/Rule 기준 계약과 충돌 없이 동작.
- 운영 문서(`docs/dev`)가 최신 계약 상태를 반영.

## 실행 결과 (2026-02-07)
### 단계별 게이트 현황
| Plan | 계약 테스트 | 핵심 헬스체크 | 통합 러너 |
|---|---|---|---|
| 0001 | pass (`3 passed`) | n/a | n/a |
| 0002 | pass (`8 passed`) | n/a | n/a |
| 0003 | pass (`12 passed`) | n/a | n/a |
| 0004 | pass (`3 passed`) | n/a | n/a |
| 0005 | pass (`3 + 11 passed`) | pass | pass (10턴/20턴 완료) |

### 현재 롤아웃 판단
- 계약 정렬(Scenario/GM/BE-router)은 배포 가능 수준.
- 실환경 E2E 게이트(10턴/20턴)까지 충족되어 단계형 롤아웃 종료 가능.
