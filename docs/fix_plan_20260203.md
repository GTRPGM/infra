# Fix Plan - 2026-02-03

## 1. 목적 (Purpose)
- GM과 Rule Engine 간의 데이터 연동 오류 및 Scenario Service 404 에러 해결.
- 시스템 전반의 ID 정합성 및 인프라 영속성 확보.

## 2. 현황 (Current Status)
- GM의 ID 매핑 및 우선순위 로직 수정 완료.
- State Manager의 스키마 및 볼륨 설정 수정 완료.
- Scenario Service의 ID 정규화 및 조회 로직 수정 완료.
- **추가 발견**: Scenario Service가 시나리오 생성 시 ID를 정규화하여 저장하지 않아, 조회 시(normalize 적용) 매칭에 실패하는 문제 발견.

## 3. 상세 계획 및 진행 로그
- Step 1: GM 데이터 구조 정규화 및 entities 통합 (완료)
- Step 2: GM HTTP Client ID 매핑 로직 개선 (Master -> Instance) (완료)
- Step 3: State Manager npc 테이블 스키마 복구 및 persistent volume 설정 (완료)
- Step 4: Scenario Service ID 정규화 로직 (언더바 -> 하이픈) 적용 (완료)
- Step 5: 테스터 로그 명칭 변경 및 에러 메시지 가독성 개선 (완료)
- Step 6: 통합 테스트 완주 확인 (진행 중 - 404 에러로 중단)
- Step 7: Scenario Service의 `get_stable_id` 로직 수정 (정규화된 ID를 정전 ID로 사용) (대기)
- Step 8: GM의 `ScenarioManagerHTTPClient` 내 ID 참조 로직 정합성 강화 (대기)

## 4. 발견된 복잡성 기록
- ID 불일치 (act_1 vs act-1) 해결을 위해 Scenario Service에 정규화 로직을 주입함.
- PostgreSQL initdb 스크립트와 서비스 로직 간의 충돌 지점(npc 테이블)을 찾아 수정함.
