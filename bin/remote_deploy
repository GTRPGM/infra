#!/bin/bash

COMMAND=$1
shift

case "$COMMAND" in
    connect)
        ssh -i $GITHUB_DEPLOY_KEY_PATH "$REMOTE_USERNAME@$REMOTE_HOST"
        ;;
    *)
        echo "Usage: project {run|run-container|test-api|lint|ci|cd}"
        exit 1
        ;;
esac
